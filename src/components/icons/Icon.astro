---
interface Props {
  name: string;
  className?: string;
}

const { name, className } = Astro.props;
---

<svg class={`icon icon-${name} ${className}`}>
  <use xlink:href={`#icon-${name}`}></use>
</svg>